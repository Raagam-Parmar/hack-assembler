(library
 (name lib)
 (modules ast parser lexer machine vector string_tools)
 (libraries menhirLib)
)

(menhir (modules parser)
 (flags --exn-carries-state --trace --dump)

  ; (flags --strict)
)

(ocamllex (modules lexer))
